<AuthorizeView>
    <Authorized>
        @if (!string.IsNullOrWhiteSpace(Src))
        {
            <iframe width="0" height="0" src="@Src"></iframe>

            // _isExpiring = false;
            // StateHasChanged();
        }
    </Authorized>
</AuthorizeView>